---
applications:
  - name: cifu-xgov-designer
    memory: 256M
    env:
      PREVIEW_MODE: true
      sandbox: true
      PERSISTENT_BACKEND: s3
      PUBLISH_URL: ((PUBLISH_URL))
      PREVIEW_URL: ((PREVIEW_URL))
